dependencies:
  override:
    - nvm install 6 && nvm use 6 && npm install && npm run build-client
    # prepublish, run with install, only builds the production version of client files
test:
  override:
    - nvm use 6 && npm test
    - nvm use 4 && npm test
